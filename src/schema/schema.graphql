type Query {
  me: User!
  user(id: ID!): User
  users: [User]!
}

type Mutation {
  signup(name: String!, email: String!, password: String!,sex:String!,phone:String!): User
}

type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  sex:String!
  phone:String!
  
}
